<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.prgrms.coffee_order_be.order.repository.OrderItemMapper">

  <!-- 단일 OrderItemEntity를 저장하는 SQL 쿼리 -->
  <insert id="save" parameterType="order_items" useGeneratedKeys="true" keyProperty="seq"
    keyColumn="seq">
    INSERT INTO order_items (order_id, product_id, category, price, quantity, created_at,
                             updated_at)
    VALUES (UUID_TO_BIN(#{order.orderId}), UUID_TO_BIN(#{product.productId}), #{category}, #{price},
        #{quantity}, #{createdAt}, #{updatedAt});
  </insert>

</mapper>